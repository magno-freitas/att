# Database Connection Properties
# These properties are now configured in secure.properties
# This file only contains non-sensitive configuration
db.driver=com.mysql.cj.jdbc.Driver
db.pool.size=10
db.pool.timeout=30000